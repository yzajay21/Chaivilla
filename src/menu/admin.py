from django.contrib import admin

# Register your models here.
from django.contrib import admin

# Register your models here.
from .models import Category,Menu_model

admin.site.register(Category)

admin.site.register(Menu_model)